def foo(arg1, arg2='bar'):
    """Does something.

    A function that does something interesting.

    Args:
        arg1 (int): First argument of foo.
        arg2 (str): Another argument of foo.

    Returns:
        nd.array:
            This is a return value.
    """
    return 0

